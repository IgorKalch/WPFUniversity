<views:MvxWindow 
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    x:Class="WpfUniversity.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:courseview="clr-namespace:WpfUniversity.Views.Courses" 
    xmlns:dialogview="clr-namespace:WpfUniversity.Views.Dialogs" 
    xmlns:vmc="clr-namespace:WpfUniversity.ViewModels.Courses"    
    xmlns:vmd="clr-namespace:WpfUniversity.ViewModels.Dialogs"    
    xmlns:custom="clr-namespace:ModalControl;assembly=ModalControl" 
    xmlns:viewmodels="clr-namespace:WpfUniversity.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
    mc:Ignorable="d"
    FontSize="16"
    Title="Courses"
    Height="600" 
    Width="1000">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vmc:AddCourseViewModel}">
            <courseview:AddCourseView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmc:EditCourseViewModel}">
            <courseview:EditCourseView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vmd:YesNoDialogViewModel}">
            <dialogview:YesNoView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <custom:Modal Panel.ZIndex="1" IsOpen="{Binding IsModalOpen}">
            <ContentControl Margin="50 25" Content="{Binding CurrentModalViewModel}" />
        </custom:Modal>
        <courseview:CourseView MaxWidth="1000" Margin="25" DataContext="{Binding CourseViewModel}" />
    </Grid>

</views:MvxWindow>

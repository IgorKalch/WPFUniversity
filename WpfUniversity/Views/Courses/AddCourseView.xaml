<UserControl x:Class="WpfUniversity.Views.Courses.AddCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:WpfUniversity.Components"
             xmlns:local="clr-namespace:WpfUniversity.Views.Courses" 
             xmlns:courses="clr-namespace:WpfUniversity.ViewModels.Courses" d:DataContext="{d:DesignInstance Type=courses:AddCourseViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeader}" Text="Add Course" />

        <components:AddCourseForm  Grid.Row="1" Margin="0 30 0 0" DataContext="{Binding AddCourseViewModel.AddCourseFormViewModel}" />
    </Grid>
</UserControl>

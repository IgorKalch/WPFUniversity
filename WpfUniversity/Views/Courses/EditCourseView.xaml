<UserControl x:Class="WpfUniversity.Views.Courses.EditCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:WpfUniversity.Components"
             xmlns:local="clr-namespace:WpfUniversity.Views.Courses" 
             xmlns:courses="clr-namespace:WpfUniversity.ViewModels.Courses" d:DataContext="{d:DesignInstance Type=courses:EditCourseViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource PageHeader}" Text="Edit Course" />

        <components:EditCourseForm  Grid.Row="1" Margin="0 30 0 0" DataContext="{Binding EditCourseFormViewModel}" />
    </Grid>
</UserControl>
